# sample-cpp-python-vscode

Simple C++/Python library with a single class.

Nothing is done with this code, the objective is to test Cmake options and
VSCode debugging.

Build with Cmake running ```bash build.sh```
